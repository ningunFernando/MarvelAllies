<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="Base.Theme.MarvelAllies" parent="Theme.Material3.Dark.NoActionBar">

        <!-- Color principal -->
        <item name="colorPrimary">@color/BackGroundBlue</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Color secundario / complementario -->
        <item name="colorSecondary">@color/LightYellow</item>
        <item name="colorOnSecondary">@color/black</item>

        <!-- Fondos -->
        <item name="android:colorBackground">@color/BackGroundBlue</item>
        <item name="colorSurface">@color/StrongerBlue</item>

        <!-- Texto -->
        <item name="android:textColorPrimary">@color/white</item>
        <item name="android:textColorSecondary">@color/OpacityWhite</item>

    </style>

</resources>
